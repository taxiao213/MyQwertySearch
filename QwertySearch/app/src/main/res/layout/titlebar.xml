<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rl"
    android:layout_width="match_parent"
    android:layout_height="45dp"
    android:background="@color/main_color">

    <!--默认隐藏-->
    <TextView
        android:id="@+id/toolbar_title_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:layout_marginLeft="10dp"
        android:text="@string/contacts"
        android:textColor="@color/white"
        android:visibility="gone"/>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/toolbar_search_iv"
            style="@style/toolbar"
            android:src="@mipmap/ic_actbar_search_blue"/>

        <ImageView
            android:id="@+id/toolbar_contacts_add_iv"
            style="@style/toolbar"
            android:src="@mipmap/ic_actbar_contact_add"/>

        <ImageView
            android:id="@+id/toolbar_call_iv"
            style="@style/toolbar"
            android:src="@mipmap/ic_actbar_phone"/>

        <ImageView
            android:id="@+id/toolbar_more_iv"
            style="@style/toolbar"
            android:src="@mipmap/ic_actbar_more"/>
    </LinearLayout>

</RelativeLayout>
